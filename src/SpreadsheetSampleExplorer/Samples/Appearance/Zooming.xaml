<UserControl x:Class="SpreadsheetSampleExplorer.Samples.Zooming"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpreadsheetSampleExplorer.Samples"
             xmlns:sheets="http://schemas.devbrewlabs.com/2026/wpf/spreadsheet"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Control Bar -->
        <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#E4E4E7" BorderThickness="1"  Padding="10,6" Margin="0,0,0,6">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Zoom:" VerticalAlignment="Center" FontSize="11.5" FontWeight="SemiBold" Foreground="#3F3F46" Margin="0,0,6,0"/>

                    <Button Content="-" Width="24" Height="24" Style="{StaticResource ModernOutlineButton}" Padding="0" Click="OnZoomOutClicked" ToolTip="Zoom Out"/>
                    <Slider x:Name="_sldZoom" Width="120" Minimum="0.1" Maximum="4.0" Value="{Binding ElementName=spread, Path=ZoomFactor, Mode=TwoWay}"
                            SmallChange="0.1" LargeChange="0.25" TickFrequency="0.1" IsSnapToTickEnabled="False" VerticalAlignment="Center" Margin="6,0"/>
                    <Button Content="+" Width="24" Height="24" Style="{StaticResource ModernOutlineButton}" Padding="0" Click="OnZoomInClicked" ToolTip="Zoom In"/>

                    <TextBlock x:Name="_txtZoomPercent" Text="100%" Width="40" TextAlignment="Right" VerticalAlignment="Center" FontSize="11.5" FontWeight="Bold" Foreground="#107C41" Margin="6,0,14,0"/>

                    <!-- Presets -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="50%" Width="40" Height="24" Style="{StaticResource ModernOutlineButton}" Padding="0" Margin="0,0,4,0" Click="OnPresetZoomClicked" Tag="0.5"/>
                        <Button Content="100%" Width="44" Height="24" Style="{StaticResource ModernOutlineButton}" Padding="0" Margin="0,0,4,0" Click="OnPresetZoomClicked" Tag="1.0"/>
                        <Button Content="150%" Width="44" Height="24" Style="{StaticResource ModernOutlineButton}" Padding="0" Margin="0,0,4,0" Click="OnPresetZoomClicked" Tag="1.5"/>
                        <Button Content="200%" Width="44" Height="24" Style="{StaticResource ModernOutlineButton}" Padding="0" Margin="0,0,4,0" Click="OnPresetZoomClicked" Tag="2.0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Shortcut Info Badge -->
                <Border DockPanel.Dock="Right" HorizontalAlignment="Right" Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💡 Shortcut: " FontWeight="Bold" Foreground="#107C41" FontSize="11"/>
                        <TextBlock Text="Ctrl + Mouse Wheel" Foreground="#15803D" FontSize="11"/>
                    </StackPanel>
                </Border>
            </DockPanel>
        </Border>

        <!-- Main Spreadsheet View -->
        <Border Grid.Row="1" BorderThickness="1" BorderBrush="#E4E4E7" >
            <sheets:Spread x:Name="spread"/>
        </Border>
    </Grid>
</UserControl>
