<UserControl x:Class="AlphaXSpreadSamplesExplorer.Samples.Formulas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AlphaXSpreadSamplesExplorer.Samples"
             xmlns:sheets="http://schemas.gcspreadsheet.com/2022/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Banner (Excel Green Gradient) -->
        <Border Grid.Row="0" CornerRadius="8" Padding="14,10" Margin="0,0,0,8">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#107C41" Offset="0"/>
                    <GradientStop Color="#0E6B38" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <TextBlock Text="Multi-Sheet Formulas &amp; Dependent Calculation Engine" 
                           Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                <TextBlock Text="Demonstrating basic, advanced, dependent, and cross-sheet interlinked formulas across multiple worksheets." 
                           Foreground="#E8F5E9" FontSize="11.5" Margin="0,3,0,0"/>
            </StackPanel>
        </Border>

        <!-- Feature Badges & Info Bar -->
        <Border Grid.Row="1" Background="#FAFAFA" BorderBrush="#E4E4E7" BorderThickness="1" CornerRadius="8" Padding="10,6" Margin="0,0,0,8">
            <WrapPanel VerticalAlignment="Center">
                <Border Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="🔗 Interlinked Sheets" Foreground="#107C41" FontSize="11" FontWeight="Medium"/>
                </Border>
                <Border Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="⚡ Dependent Formulas" Foreground="#107C41" FontSize="11" FontWeight="Medium"/>
                </Border>
                <Border Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="📊 SUM, AVG, MIN, MAX, COUNT" Foreground="#107C41" FontSize="11" FontWeight="Medium"/>
                </Border>
                <Border Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="🔄 Real-Time Recalculation" Foreground="#107C41" FontSize="11" FontWeight="Medium"/>
                </Border>
                <TextBlock Text="Try editing any value in 'Regional Sales' to see 'Summary Dashboard' update!" 
                           Foreground="#71717A" FontSize="11" FontStyle="Italic" VerticalAlignment="Center" Margin="6,0,0,0"/>
            </WrapPanel>
        </Border>

        <!-- Main Spread Control -->
        <Border Grid.Row="2" BorderThickness="1" BorderBrush="#E4E4E7" CornerRadius="6">
            <sheets:AlphaXSpread x:Name="spread"/>
        </Border>
    </Grid>
</UserControl>
