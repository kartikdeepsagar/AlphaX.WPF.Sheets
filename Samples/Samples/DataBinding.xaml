<UserControl x:Class="AlphaXSpreadSamplesExplorer.Samples.DataBinding"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AlphaXSpreadSamplesExplorer.Samples" 
             xmlns:sheets="http://schemas.gcspreadsheet.com/2022/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Column 1: Observable / Generic List Binding -->
        <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E4E4E7" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,6,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="C# POCO List Binding" FontSize="14" FontWeight="SemiBold" Foreground="#18181B"/>
                        <Border Background="#F4F4F5" BorderBrush="#E4E4E7" BorderThickness="1" CornerRadius="4" Padding="6,1" Margin="8,0,0,0">
                            <TextBlock Text="List&lt;T&gt;" FontSize="10" FontWeight="Medium" Foreground="#52525B"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="Bound directly to an in-memory collection of Customer objects." FontSize="11" Foreground="#71717A" Margin="0,2,0,0"/>
                </StackPanel>

                <Border Grid.Row="1" BorderThickness="1" BorderBrush="#E4E4E7" CornerRadius="6" Background="White">
                    <sheets:AlphaXSpread x:Name="spread"/>
                </Border>
            </Grid>
        </Border>

        <!-- Column 2: DataTable Binding -->
        <Border Grid.Column="1" Background="#FAFAFA" BorderBrush="#E4E4E7" BorderThickness="1" CornerRadius="8" Padding="10" Margin="6,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="ADO.NET DataTable Binding" FontSize="14" FontWeight="SemiBold" Foreground="#18181B"/>
                        <Border Background="#F4F4F5" BorderBrush="#E4E4E7" BorderThickness="1" CornerRadius="4" Padding="6,1" Margin="8,0,0,0">
                            <TextBlock Text="DataTable" FontSize="10" FontWeight="Medium" Foreground="#52525B"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="Bound to a dynamic schema DataTable with two-way synchronisation." FontSize="11" Foreground="#71717A" Margin="0,2,0,0"/>
                </StackPanel>

                <Border Grid.Row="1" BorderThickness="1" BorderBrush="#E4E4E7" CornerRadius="6" Background="White">
                    <sheets:AlphaXSpread x:Name="spread2"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
