<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Excel-Inspired Material Palette Brushes -->
    <SolidColorBrush x:Key="WindowBg" Color="#FAFAFA"/>
    <SolidColorBrush x:Key="AppHeaderBg" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="HeaderBorder" Color="#E4E4E7"/>
    
    <!-- Sidebar Surface -->
    <SolidColorBrush x:Key="SidebarBg" Color="#F4F4F5"/>
    <SolidColorBrush x:Key="SidebarBorder" Color="#E4E4E7"/>
    <SolidColorBrush x:Key="SidebarItemHoverBg" Color="#E8F5E9"/>
    <SolidColorBrush x:Key="SidebarItemSelectedBg" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SidebarTextPrimary" Color="#18181B"/>
    <SolidColorBrush x:Key="SidebarTextSecondary" Color="#71717A"/>
    
    <!-- Excel Brand Green Accent -->
    <SolidColorBrush x:Key="ExcelGreen" Color="#107C41"/>
    <SolidColorBrush x:Key="ExcelGreenHover" Color="#0E6B38"/>
    <SolidColorBrush x:Key="ExcelGreenPressed" Color="#0B522B"/>
    <SolidColorBrush x:Key="ExcelSoftBg" Color="#F0FDF4"/>
    <SolidColorBrush x:Key="ExcelSoftBorder" Color="#BBF7D0"/>
    
    <!-- Main Content & Cards -->
    <SolidColorBrush x:Key="MainBg" Color="#FAFAFA"/>
    <SolidColorBrush x:Key="CardBg" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="CardBorder" Color="#E4E4E7"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#18181B"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#52525B"/>
    <SolidColorBrush x:Key="TextMuted" Color="#A1A1AA"/>

    <!-- Shared Fonts -->
    <FontFamily x:Key="AppFont">Segoe UI, Segoe UI Variable, Inter, sans-serif</FontFamily>

    <!-- Soft Material Drop Shadows -->
    <DropShadowEffect x:Key="SubtleShadow" BlurRadius="16" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.04"/>
    <DropShadowEffect x:Key="HeaderShadow" BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.03"/>

    <!-- Modern Excel Green Primary Button Style -->
    <Style x:Key="ModernButton" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource ExcelGreen}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,6"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="6" 
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ExcelGreenHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ExcelGreenPressed}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#E4E4E7"/>
                            <Setter Property="Foreground" Value="#A1A1AA"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Material Outline / Secondary Button Style -->
    <Style x:Key="ModernOutlineButton" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="#107C41"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#BBF7D0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="6" 
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#F0FDF4"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#86EFAC"/>
                            <Setter Property="Foreground" Value="#0E6B38"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#DCFCE7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Minimal Material Search / TextBox Style -->
    <Style x:Key="ModernTextBox" TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#18181B"/>
        <Setter Property="Background" Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="#E4E4E7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="CaretBrush" Value="#107C41"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="6" 
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#BBF7D0"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#107C41"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern ComboBox Style -->
    <Style x:Key="ModernComboBox" TargetType="ComboBox">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#18181B"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E4E4E7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <!-- Custom GridSplitter Style -->
    <Style x:Key="ModernGridSplitter" TargetType="GridSplitter">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="6"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="Transparent">
                        <Border x:Name="SplitterLine" Width="2" HorizontalAlignment="Center" Background="#E4E4E7" CornerRadius="1"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SplitterLine" Property="Background" Value="#107C41"/>
                            <Setter TargetName="SplitterLine" Property="Width" Value="3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Material Card Border Container -->
    <Style x:Key="ModernCard" TargetType="Border">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="#E4E4E7"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Effect" Value="{StaticResource SubtleShadow}"/>
    </Style>

</ResourceDictionary>
